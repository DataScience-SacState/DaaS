function randomNum(p1, p2) {
 var randomNumber = Math.floor(Math.random() * (10)) + 1;


    return randomNumber;              // The function returns the product of p1 and p2
}
module.exports = randomNum;
